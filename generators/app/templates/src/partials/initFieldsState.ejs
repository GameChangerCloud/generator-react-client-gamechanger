<%_ type.fields.forEach(field => { _%>
    <%_ if(field.type !== "ID"){ _%>
    <%_ if (field.type === "String" || field.type === "Int" || field.type === "Boolean" || scalars.includes(field.type)) { // Temporary, until we deal with relationship _%>
        <%_ if (field.type === "Boolean") { _%>
        <%- field.name %>: true, <%_ } else if(field.type === "HexColorCode") { _%>
        <%- field.name %>: "#fff" <%_ } else if(field.type === "RGB" || field.type === "RGBA") { _%>
        <%- field.name %>: {r: 255, g: 255, b: 255, a: 1} <%_ } else if(field.type === "HSL" || field.type === "HSLA") { _%>
        <%- field.name %>: {h: 255, s: 255, l: 255, a: 1} <%_ } else { _%>
        <%- field.name %>: '', <%_ } _%>
    <%_ } else { _%>
        <%_ if (field.isArray) { _%>
        <%- field.name %> : [], <% } else { _%>
        <%- field.name %>: null, <% } _%>
    <%_ } _%>

    <%_ } _%>
<%_ }) _%>
        redirect: null
